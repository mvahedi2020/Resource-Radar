import {Component,type ReactNode} from 'react';
export class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return {failed:true};}render(){return this.state.failed?<main><h1>The sample workspace could not load.</h1><p>You can still read the product case study or reset incompatible saved data.</p><a href="https://github.com/mvahedi2020/Resource-Radar/blob/main/docs/product/Case_Study.md">Read the case study</a><button onClick={()=>{try{localStorage.removeItem('northstar.resource-radar.plan.v1')}catch{/* storage may be blocked */}location.reload()}}>Reset sample workspace</button></main>:this.props.children;}}
